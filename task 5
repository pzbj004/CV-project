这周是模型集合。首先，模型方法包括Stacking，Bagging和Boosting。Dropout在每次training时，都会随机扔掉一些点，但是仍然会到所有的点，这是一个有效的减少
过拟合的办法，同时可以在预测时增加模型的精度。
因为采集数据比较困难，或者费用太高，我们经常会用到数据扩增，训练时可以用数据扩增，预测时也可以用数据扩增。 5.3.3 Snapshot 里面的加入应该是打错了，应该是
假如。
集成模型对计算量要求比较高，Dropout和TTA在所有场合都可以起作用。
