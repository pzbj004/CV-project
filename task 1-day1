1.对赛题的理解
任务是识别街道字符。现在给了一些包含街道字符的照片，这些照片已经被分好了，训练集包含3w张，验证集包含1w张，测试集包含4w张。因此，我们
不需要自己把这些照片分成traingin set, validation set, 和test set。但是我比较好奇，为什么要分4w张给测试集。
赛题来源于SVHN(英文名是The street view house)，可以参考相关的paper。

2.对数据的理解
每张照片里面的字符已经有了位置框。用top和left来描述字符框左上角的位置，用width和height来描述照片的宽度和高度，label是字符编码。同一张照片会有
一个或多个字符，因此在标注中，会有一个或者多个边框信息。

3.评测指标
正确识别的照片在整个test的照片中的占比，一张照片里面所有字符都被正确识别，这张照片才算正确识别。

4.解题思路
不同的照片包含的字符数不同，这是识别的难点。
4.1 定长字符识别：因为每张照片字符最多是六个，字符不够六个的用X填充。这样，所有照片都包含六个字符，一共有0,1,2...,9,X 十一种类别的字符，X代表是空字符。

4.2 不定长字符识别：CRNN可以被用来解决不定长的字符识别问题

4.3 检测再识别：可以参考物体检测模型用SSD或者YOLO来完成。

5.小节
这是一个字符识别问题，可以先用定长字符识别这个简单一点的思路，再尝试后面两种思路。
